"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.googletrans = googletrans;
exports.translate = translate;
exports.getResult = getResult;
var qs_1 = __importDefault(require("qs"));
var axios_1 = __importDefault(require("axios"));
// import adapter from "axios/lib/adapters/http";
var languages_1 = require("./languages");
var googleToken_1 = require("./googleToken");
var utils_1 = require("./utils");
function getStringOption(value, field) {
    if (typeof value === "undefined") {
        return undefined;
    }
    if (typeof value !== "string") {
        throw new Error("The language option \"".concat(field, "\" must be a string."));
    }
    return value;
}
function getSafeTld(value) {
    if (typeof value === "undefined") {
        return "com";
    }
    if (typeof value !== "string") {
        throw new Error("The option \"tld\" must be a string.");
    }
    var normalized = value.trim().toLowerCase();
    var TLD_PATTERN = /^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)*$/;
    if (!TLD_PATTERN.test(normalized)) {
        throw new Error("The option \"tld\" must be a valid Google Translate domain suffix.");
    }
    return normalized;
}
function getResponseBody(res) {
    if (!res || res.status !== 200 || !Array.isArray(res.data) || !Array.isArray(res.data[0])) {
        throw new Error("Unexpected response format from Google Translate.");
    }
    return res.data;
}
/**
 * Translation
 * @param text - The text to be translated.
 * @param options - The translation options. If the param is string, mean the language you want to translate into. If the param is object，can set more options.
 */
function googletrans(text, options) {
    var a;
    if (typeof options === "string") {
        a = { to: options };
    }
    else {
        a = options;
    }
    return translate(text, a);
}
/**
 * @param {string} text - The text to be translated
 * @param {Object} opts - Options
 * @return {Promise} - Axios Promise
 */
function translate(text, opts) {
    return __awaiter(this, void 0, void 0, function () {
        var _opts, _text, e, from, to, tld, str, i, t, e_1, URL, TOKEN, PARAMS, HEADERS, res;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    _opts = __assign({}, (opts || {}));
                    _text = text;
                    from = getStringOption(_opts.from, "from");
                    to = getStringOption(_opts.to, "to");
                    tld = getSafeTld(_opts.tld);
                    [from, to].forEach(function (lang) {
                        if (lang && !(0, languages_1.isSupported)(lang)) {
                            e = new Error("The language \u300C".concat(lang, "\u300Dis not suppored!"));
                            throw e;
                        }
                    });
                    if (Array.isArray(_text)) {
                        str = "";
                        for (i = 0; i < _text.length; i++) {
                            t = _text[i];
                            if (t.length === 0 && i === 0) {
                                e_1 = new Error("The first element of the text array is an empty string.");
                                throw e_1;
                            }
                            else {
                                str += t + "\n";
                            }
                        }
                        _text = str;
                    }
                    if (_text.length === 0) {
                        e = new Error("The text to be translated is empty!");
                        throw e;
                    }
                    if (_text.length > 15000) {
                        e = new Error("The text is over the maximum character limit ( 15k )!");
                        throw e;
                    }
                    _opts.from = (0, languages_1.getCode)(from || "auto");
                    _opts.to = (0, languages_1.getCode)(to || "en");
                    _opts.tld = tld;
                    _opts.client = _opts.client || "t";
                    URL = "https://translate.google." + _opts.tld + "/translate_a/single";
                    TOKEN = (0, googleToken_1.getToken)(_text);
                    PARAMS = {
                        client: _opts.client,
                        sl: _opts.from,
                        tl: _opts.to,
                        hl: "en",
                        dt: ["at", "bd", "ex", "ld", "md", "qca", "rw", "rm", "ss", "t"],
                        ie: "UTF-8",
                        oe: "UTF-8",
                        otf: 1,
                        ssel: 0,
                        tsel: 0,
                        kc: 7,
                        q: _text,
                        tk: TOKEN,
                    };
                    HEADERS = {
                        "User-Agent": (0, utils_1.getUserAgent)(),
                        "Accept-Encoding": "gzip",
                    };
                    return [4 /*yield*/, (0, axios_1.default)({
                            // adapter,
                            url: URL,
                            params: PARAMS,
                            headers: HEADERS,
                            timeout: 3 * 1000,
                            signal: _opts.signal,
                            paramsSerializer: function (params) {
                                return qs_1.default.stringify(params, { arrayFormat: "repeat" });
                            },
                        })];
                case 1:
                    res = _a.sent();
                    return [2 /*return*/, getResult(res)];
            }
        });
    });
}
function getResult(res) {
    var result = {
        text: "",
        textArray: [],
        pronunciation: "",
        hasCorrectedLang: false,
        src: "",
        hasCorrectedText: false,
        correctedText: "",
        translations: [],
        raw: [],
    };
    if (res === null)
        return result;
    var body = getResponseBody(res);
    result.raw = body;
    body[0].forEach(function (obj) {
        if (!Array.isArray(obj)) {
            return;
        }
        if (typeof obj[0] === "string") {
            result.text += obj[0];
        }
        if (typeof obj[2] === "string") {
            result.pronunciation += obj[2];
        }
    });
    var detectedSource = typeof body[2] === "string" ? body[2] : "";
    var correctedSource = Array.isArray(body[8]) && Array.isArray(body[8][0]) && typeof body[8][0][0] === "string"
        ? body[8][0][0]
        : detectedSource;
    result.src = correctedSource;
    result.hasCorrectedLang = Boolean(detectedSource && correctedSource && detectedSource !== correctedSource);
    if (Array.isArray(body[1]) && Array.isArray(body[1][0]) && body[1][0][2]) {
        result.translations = body[1][0][2];
    }
    if (Array.isArray(body[7]) && typeof body[7][0] === "string") {
        var str = body[7][0];
        str = str.replace(/<b><i>/g, "[");
        str = str.replace(/<\/i><\/b>/g, "]");
        result.correctedText = str;
        if (body[7][5])
            result.hasCorrectedText = true;
    }
    if (result.text.indexOf("\n") !== -1) {
        result.textArray = result.text.split("\n");
    }
    else {
        result.textArray.push(result.text);
    }
    return result;
}
exports.default = googletrans;
