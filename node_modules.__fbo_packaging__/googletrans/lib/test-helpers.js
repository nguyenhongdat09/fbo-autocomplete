"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.testConsole = void 0;
exports.withNetworkRetries = withNetworkRetries;
exports.createRetriableAsyncFn = createRetriableAsyncFn;
exports.expectTextVariant = expectTextVariant;
var RETRYABLE_ERROR_CODES = new Set(["ECONNRESET", "ECONNABORTED", "ENOTFOUND", "ETIMEDOUT"]);
var RETRYABLE_STATUS_CODES = new Set([429, 500, 502, 503, 504]);
function sleep(ms) {
    return new Promise(function (resolve) { return setTimeout(resolve, ms); });
}
function isJestAssertionError(error) {
    return error instanceof Error && "matcherResult" in error;
}
function isRetryableNetworkError(error) {
    var _a;
    if (!(error instanceof Error)) {
        return false;
    }
    var maybeError = error;
    if (maybeError.code && RETRYABLE_ERROR_CODES.has(maybeError.code)) {
        return true;
    }
    if (((_a = maybeError.response) === null || _a === void 0 ? void 0 : _a.status) && RETRYABLE_STATUS_CODES.has(maybeError.response.status)) {
        return true;
    }
    var message = maybeError.message.toLowerCase();
    return (message.includes("timeout") ||
        message.includes("socket hang up") ||
        message.includes("network error") ||
        message.includes("temporary"));
}
function withNetworkRetries(run_1) {
    return __awaiter(this, arguments, void 0, function (run, attempts) {
        var lastError, attempt, error_1;
        if (attempts === void 0) { attempts = 3; }
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    attempt = 0;
                    _a.label = 1;
                case 1:
                    if (!(attempt < attempts)) return [3 /*break*/, 7];
                    _a.label = 2;
                case 2:
                    _a.trys.push([2, 4, , 6]);
                    return [4 /*yield*/, run()];
                case 3: return [2 /*return*/, _a.sent()];
                case 4:
                    error_1 = _a.sent();
                    if (!isRetryableNetworkError(error_1) || attempt === attempts - 1) {
                        throw error_1;
                    }
                    lastError = error_1;
                    return [4 /*yield*/, sleep(250 * (attempt + 1))];
                case 5:
                    _a.sent();
                    return [3 /*break*/, 6];
                case 6:
                    attempt += 1;
                    return [3 /*break*/, 1];
                case 7: throw lastError;
            }
        });
    });
}
function createRetriableAsyncFn(fn, attempts) {
    if (attempts === void 0) { attempts = 3; }
    return function () {
        var args = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
        }
        return withNetworkRetries(function () { return fn.apply(void 0, args); }, attempts);
    };
}
function expectTextVariant(actual, expected) {
    var variants = Array.isArray(expected) ? expected : [expected];
    expect(variants).toContain(actual);
}
exports.testConsole = {
    log: function (value) {
        if (isJestAssertionError(value)) {
            throw value;
        }
        globalThis.console.log(value);
    },
};
