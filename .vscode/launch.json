{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Extension (Antigravity)",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ]
        }
    ]
}